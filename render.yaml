services:
  - type: web
    name: telegram-bot-banksoal
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
